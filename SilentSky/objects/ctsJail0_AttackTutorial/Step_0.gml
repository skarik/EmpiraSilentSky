// If the player does an attack, then we go back to the game
if (objPlayerPaladin.moState == MO_MELEE0 || objPlayerPaladin.moState == MO_MELEESTAND0 || objPlayerPaladin.moState == MO_MELEEGND0)
{
    fade = true;
}

