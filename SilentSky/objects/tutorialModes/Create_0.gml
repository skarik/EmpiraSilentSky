event_inherited();
if (!Game.tuShowModes) idelete(this);

start_count = levelManager.lvCurrentLimit + instance_number(enemyBase) + instance_number(bloxDestroyable) + instance_number(pickupBase) - 1;

